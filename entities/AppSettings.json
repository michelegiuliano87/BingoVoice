{
  "name": "AppSettings",
  "type": "object",
  "properties": {
    "bonus_audio_url": {
      "type": "string",
      "description": "URL of the audio played during the Bonus Voice screen"
    },
    "bonus_video_url": {
      "type": "string",
      "description": "URL of the video played before the Bonus Voice image reveal"
    },
    "background_music_url": {
      "type": "string",
      "description": "URL of the background music played during the game"
    },
    "wheel_audio_url": {
      "type": "string",
      "description": "URL of the audio played during the Lucky Wheel spin"
    },
    "panariello_band_video_url": {
      "type": "string",
      "description": "URL of the video played for Panariello Band items"
    },
    "waiting_video_url": {
      "type": "string",
      "description": "URL of the looping video shown on the projection screen while waiting for extractions"
    }
  },
  "required": []
}